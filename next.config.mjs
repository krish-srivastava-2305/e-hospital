/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.pinimg.com",
      "images.unsplash.com",
      "media.istockphoto.com",
      "plus.unsplash.com",
    ],
  },
};

export default nextConfig;
